import * as React from "react";
import * as Types from "../types";

declare function LabelDefaultInverse(
    props: {
        as?: React.ElementType;
        text?: React.ReactNode;
    }
): React.JSX.Element